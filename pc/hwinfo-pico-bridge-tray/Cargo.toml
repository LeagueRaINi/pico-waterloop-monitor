[package]
name = "hwinfo-pico-bridge-tray"
version.workspace = true
edition.workspace = true
description = "Silent front end with a tray icon: what runs at login"

[[bin]]
name = "hwinfo-pico-bridge-tray"
path = "src/main.rs"

[dependencies]
clap = { version = "4.6", features = ["derive"] }
hwinfo-pico-bridge-core = { path = "../hwinfo-pico-bridge-core" }
